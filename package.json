{
	"name": "promise-gate",
	"version": "1.1.1",
	"description": "wrap a function in a 'gate' function that must run first",
	"main": "promise-gate.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"repository": {
		"type": "git",
		"url": "https://archive.voodoowarez.com/promise-gate"
	},
	"keywords": ["promise", "gate", "wrap"],
	"author": "rektide de la faye",
	"license": "X11",
	"dependencies": {
		"defaulter": "^1.1.0",
		"new-apply": "^1.0.0"
	}
}
